name: healthcare_continuum_app
description: Privacy-first healthcare continuum mobile interface.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+1
  google_mlkit_face_detection: ^0.11.0
  record: ^5.1.2
  http: ^1.2.2
  flutter_riverpod: ^2.5.1

flutter:
  uses-material-design: true
